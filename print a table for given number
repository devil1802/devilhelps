#include<stdio.h>
int main()
{
	int num,i;
	//input from user
	printf("Enter a number:");
	scanf("%d",&num);
	//print multiplication table using for loop
	printf
	printf("multiplication table of %d:\n",num);
	for(i=1;1<=10;i++)
	{
		printf("%d%d=%d\n",num,i,num*i);
	}
	return 0;
}
